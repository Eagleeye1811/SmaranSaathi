# Phase 1 — FastAPI skeleton
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0

# Testing
pytest>=8.3
httpx>=0.27

# ── Phase 2 (Firebase Auth + Firestore) ────────────────────────────────────
firebase-admin>=6.5

# ── Phase 3 (device sync tokens) ───────────────────────────────────────────
pyjwt>=2.9
