<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only (this whole source set is), so release stays HTTPS-only.

     The WebView follows Android's cleartext policy even though Dart's HttpClient
     does not, so without this the Asha page served by a local backend over plain
     http:// fails with ERR_CLEARTEXT_NOT_PERMITTED. 10.0.2.2 is the host machine
     as seen from the Android emulator. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
